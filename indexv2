<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aaron Lattimore — Screenwriter & Copywriter</title>
    <meta name="description" content="Aaron Lattimore — Screenwriter and copywriter with 8+ years of experience crafting compelling narratives for visual media and performance marketing.">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=Instrument+Serif:ital@0;1&family=DM+Mono:ital,wght@0,300;0,400;1,300&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        /* ===== RESET ===== */
        *, *::before, *::after {
            margin: 0; padding: 0; box-sizing: border-box;
        }

        /* ===== PORTFOLIO VARIABLES ===== */
        :root {
            --deep-black: #050510;
            --dark-blue: #0a0a2e;
            --mid-blue: #0f1b4d;
            --accent-blue: #1e3a8a;
            --light-blue: #3b82f6;
            --glow-blue: #60a5fa;
            --ghost-white: #e8eaf6;
            --dim-white: #9ca3af;
            --warm-white: #f1f5f9;

            /* Entry page variables */
            --bg-primary: #0a0a0f;
            --bg-card: #12121a;
            --bg-card-border: #1e1e2e;
            --accent: #6c63ff;
            --accent-glow: rgba(108, 99, 255, 0.3);
            --accent-green: #22c55e;
            --accent-green-glow: rgba(34, 197, 94, 0.35);
            --text-primary: #f0f0f5;
            --text-secondary: #8888a0;
            --text-muted: #555566;
        }

        html { scroll-behavior: smooth; }

        body {
            font-family: 'Syne', sans-serif;
            background: var(--bg-primary);
            color: var(--ghost-white);
            overflow-x: hidden;
            cursor: default;
        }

        /* =============================================
           ENTRY SCREEN
        ============================================= */
        #entryScreen {
            position: fixed;
            inset: 0;
            z-index: 50000;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
            transition: opacity 0.7s ease, transform 0.7s ease;
        }

        #entryScreen.fade-out {
            opacity: 0;
            transform: scale(1.04);
            pointer-events: none;
        }

        #entryScreen.gone { display: none; }

        /* Entry background grid */
        #entryScreen::before {
            content: '';
            position: absolute;
            inset: 0;
            background-image:
                linear-gradient(rgba(108,99,255,0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(108,99,255,0.03) 1px, transparent 1px);
            background-size: 60px 60px;
            animation: gridShift 20s linear infinite;
            pointer-events: none;
        }
        @keyframes gridShift { 0%{transform:translate(0,0)} 100%{transform:translate(60px,60px)} }

        .entry-orb { position: absolute; border-radius: 50%; filter: blur(100px); opacity: 0.4; pointer-events: none; }
        .entry-orb-1 { width:400px; height:400px; background:var(--accent); top:-100px; left:-100px; animation:efloat1 12s ease-in-out infinite; }
        .entry-orb-2 { width:300px; height:300px; background:#8b5cf6; bottom:-80px; right:-80px; animation:efloat2 15s ease-in-out infinite; }
        .entry-orb-3 { width:200px; height:200px; background:#3b82f6; top:50%; left:60%; animation:efloat3 10s ease-in-out infinite; }
        @keyframes efloat1 { 0%,100%{transform:translate(0,0) scale(1)} 50%{transform:translate(80px,60px) scale(1.1)} }
        @keyframes efloat2 { 0%,100%{transform:translate(0,0) scale(1)} 50%{transform:translate(-60px,-40px) scale(1.15)} }
        @keyframes efloat3 { 0%,100%{transform:translate(0,0) scale(1)} 50%{transform:translate(-40px,30px) scale(0.9)} }

        .entry-card-container { position: relative; z-index: 10; }
        .entry-card {
            background: var(--bg-card);
            border: 1px solid var(--bg-card-border);
            border-radius: 20px;
            padding: 56px 64px;
            max-width: 580px;
            width: 90vw;
            text-align: center;
            box-shadow: 0 4px 60px rgba(0,0,0,0.5), 0 0 80px var(--accent-glow);
            position: relative;
            overflow: visible;
            animation: cardEntry 0.8s cubic-bezier(0.16,1,0.3,1) both;
        }
        @keyframes cardEntry { from{opacity:0;transform:translateY(40px) scale(0.95)} to{opacity:1;transform:translateY(0) scale(1)} }
        .entry-card::before {
            content:''; position:absolute; inset:-1px; border-radius:20px; padding:1px;
            background: linear-gradient(135deg, rgba(108,99,255,0.4), transparent 50%, rgba(139,92,246,0.2));
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor; mask-composite: exclude; pointer-events: none;
        }

        .entry-icon-row { margin-bottom:24px; font-size:40px; animation:iconPulse 2s ease-in-out infinite; }
        @keyframes iconPulse { 0%,100%{transform:scale(1)} 50%{transform:scale(1.08)} }

        .entry-question {
            font-size:1.55rem; font-weight:600; line-height:1.4; margin-bottom:12px;
            background: linear-gradient(135deg, var(--text-primary), #c4b5fd);
            -webkit-background-clip:text; -webkit-text-fill-color:transparent; background-clip:text;
            font-family: 'Inter', sans-serif;
        }
        .entry-subtitle { font-size:0.88rem; color:var(--text-secondary); margin-bottom:40px; font-family: 'Inter', sans-serif; }

        .entry-arena {
            position:relative; display:flex; align-items:center; justify-content:center;
            gap:24px; min-height:80px; transition: min-height 0.4s ease;
        }
        .entry-btn {
            font-family:'Inter',sans-serif; font-weight:600; font-size:1.05rem; border:none;
            border-radius:12px; cursor:pointer; transition: all 0.35s cubic-bezier(0.16,1,0.3,1);
            letter-spacing:0.02em; outline:none; user-select:none;
        }
        .entry-btn-yes {
            padding:16px 48px;
            background:linear-gradient(135deg, var(--accent-green), #16a34a);
            color:#fff; box-shadow:0 4px 24px var(--accent-green-glow); z-index:2;
        }
        .entry-btn-yes:hover { box-shadow:0 6px 36px var(--accent-green-glow), 0 0 60px var(--accent-green-glow); }
        .entry-btn-no {
            padding:14px 40px;
            background:linear-gradient(135deg, #2a2a3a, #1e1e2e);
            color:var(--text-secondary); border:1px solid #333348;
            box-shadow:0 2px 12px rgba(0,0,0,0.3); z-index:2;
        }

        .entry-confession { display:none; margin-top:24px; animation:fadeUp 0.6s ease both; }
        .entry-confession.show { display:block; }
        @keyframes fadeUp { from{opacity:0;transform:translateY(10px)} to{opacity:1;transform:translateY(0)} }
        .entry-confession p { color:var(--text-secondary); font-size:0.9rem; line-height:1.6; font-family: 'Inter', sans-serif; }
        .entry-confession .entry-portfolio-btn {
            display:inline-block; margin-top:16px; padding:12px 32px;
            background:linear-gradient(135deg, var(--accent), #8b5cf6); color:#fff;
            font-weight:600; font-size:0.95rem; border-radius:10px; border: none;
            box-shadow:0 4px 24px var(--accent-glow); transition:all 0.3s ease;
            cursor: pointer; font-family: 'Inter', sans-serif; letter-spacing: 0.02em;
        }
        .entry-confession .entry-portfolio-btn:hover { box-shadow:0 6px 36px var(--accent-glow); transform:translateY(-2px); }

        .entry-dodge-counter {
            position:absolute; bottom:-48px; left:50%; transform:translateX(-50%);
            font-size:0.78rem; color:var(--text-muted); white-space:nowrap;
            opacity:0; transition:opacity 0.4s ease; font-family: 'Inter', sans-serif;
        }
        .entry-dodge-counter.visible { opacity:1; }

        .entry-particle {
            position:fixed; width:8px; height:8px; border-radius:50%;
            pointer-events:none; z-index:100; animation:particleBurst 0.8s ease-out forwards;
        }
        @keyframes particleBurst {
            0%{transform:translate(0,0) scale(1);opacity:1}
            100%{transform:translate(var(--tx),var(--ty)) scale(0);opacity:0}
        }

        @media(max-width:600px){
            .entry-card{padding:40px 28px} .entry-question{font-size:1.25rem}
            .entry-btn-yes{padding:14px 36px;font-size:0.95rem} .entry-btn-no{padding:12px 28px;font-size:0.9rem}
        }

        /* =============================================
           PORTFOLIO SCREEN
        ============================================= */
        #portfolioScreen {
            display: none;
            opacity: 0;
            transition: opacity 0.7s ease;
        }
        #portfolioScreen.show {
            display: block;
            opacity: 1;
        }

        /* Custom cursor trail */
        .cursor-dot {
            position: fixed; width: 6px; height: 6px;
            background: var(--glow-blue); border-radius: 50%;
            pointer-events: none; z-index: 9999; opacity: 0;
            transition: opacity 0.3s; mix-blend-mode: screen;
        }
        body:hover .cursor-dot { opacity: 1; }

        .cursor-ring {
            position: fixed; width: 36px; height: 36px;
            border: 1.5px solid rgba(96, 165, 250, 0.4);
            border-radius: 50%; pointer-events: none; z-index: 9998; opacity: 0;
            transition: opacity 0.3s, width 0.2s, height 0.2s, border-color 0.2s;
            mix-blend-mode: screen;
        }
        body:hover .cursor-ring { opacity: 1; }
        .cursor-ring.hovering { width: 56px; height: 56px; border-color: var(--glow-blue); }

        /* Noise overlay */
        #portfolioScreen::before {
            content: ''; position: fixed; top: 0; left: 0;
            width: 100%; height: 100%;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.03'/%3E%3C/svg%3E");
            pointer-events: none; z-index: 10000;
        }

        /* Particles */
        .particles-container {
            position: fixed; top: 0; left: 0;
            width: 100%; height: 100%; pointer-events: none; z-index: 0;
        }
        .particle {
            position: absolute; width: 2px; height: 2px;
            background: var(--light-blue); border-radius: 50%;
            opacity: 0; animation: particleFloat linear infinite;
        }
        @keyframes particleFloat {
            0% { transform: translateY(100vh) scale(0); opacity: 0; }
            10% { opacity: 0.6; }
            90% { opacity: 0.6; }
            100% { transform: translateY(-10vh) scale(1); opacity: 0; }
        }

        /* HERO */
        .hero {
            min-height: 100vh; display: flex; flex-direction: column;
            justify-content: center; align-items: center;
            position: relative; padding: 2rem; overflow: hidden;
        }
        .hero-bg-gradient {
            position: absolute; top: 50%; left: 50%;
            transform: translate(-50%, -50%);
            width: 140vmax; height: 140vmax;
            background: radial-gradient(ellipse at center,
                rgba(30, 58, 138, 0.15) 0%, rgba(15, 27, 77, 0.08) 35%, transparent 70%);
            animation: pulseGlow 8s ease-in-out infinite alternate;
        }
        @keyframes pulseGlow {
            0% { transform: translate(-50%, -50%) scale(1); opacity: 0.8; }
            100% { transform: translate(-50%, -50%) scale(1.15); opacity: 1; }
        }

        .orb {
            position: absolute; border-radius: 50%;
            filter: blur(80px); opacity: 0.12;
            animation: orbDrift 20s ease-in-out infinite alternate;
        }
        .orb-1 { width:500px; height:500px; background:var(--accent-blue); top:10%; left:-10%; animation-delay:0s; }
        .orb-2 { width:400px; height:400px; background:var(--mid-blue); bottom:5%; right:-5%; animation-delay:-7s; animation-duration:25s; }
        .orb-3 { width:300px; height:300px; background:var(--light-blue); top:60%; left:50%; animation-delay:-14s; animation-duration:18s; }
        @keyframes orbDrift {
            0% { transform: translate(0,0) rotate(0deg); }
            33% { transform: translate(50px,-30px) rotate(120deg); }
            66% { transform: translate(-30px,50px) rotate(240deg); }
            100% { transform: translate(20px,-20px) rotate(360deg); }
        }

        .hero-content { position: relative; z-index: 2; text-align: center; max-width: 900px; }

        .greeting {
            font-family: 'DM Mono', monospace; font-size: 0.85rem;
            letter-spacing: 0.3em; text-transform: uppercase;
            color: var(--glow-blue); margin-bottom: 1.5rem;
            opacity: 0; animation: fadeSlideUp 0.8s ease forwards; animation-delay: 0.3s;
        }
        .hero-name {
            font-family: 'Instrument Serif', serif;
            font-size: clamp(3.5rem, 10vw, 8rem);
            font-weight: 400; line-height: 0.95; letter-spacing: -0.02em;
            margin-bottom: 0.5rem;
            background: linear-gradient(135deg, var(--warm-white) 0%, var(--glow-blue) 50%, var(--warm-white) 100%);
            background-size: 200% auto;
            -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
            animation: fadeSlideUp 1s ease forwards, shimmerText 6s ease-in-out infinite;
            animation-delay: 0.6s, 2s;
        }
        @keyframes shimmerText {
            0% { background-position: 0% center; }
            50% { background-position: 200% center; }
            100% { background-position: 0% center; }
        }
        .hero-title {
            font-family: 'DM Mono', monospace; font-size: clamp(0.75rem, 1.5vw, 1rem);
            font-weight: 300; letter-spacing: 0.2em; color: var(--dim-white);
            text-transform: uppercase; margin-bottom: 2.5rem;
            opacity: 0; animation: fadeSlideUp 0.8s ease forwards; animation-delay: 1s;
        }
        .hero-blurb {
            font-family: 'Syne', sans-serif; font-size: clamp(0.95rem, 1.8vw, 1.15rem);
            line-height: 1.8; color: var(--dim-white); max-width: 700px;
            margin: 0 auto 3.5rem;
            opacity: 0; animation: fadeSlideUp 0.8s ease forwards; animation-delay: 1.3s;
        }
        @keyframes fadeSlideUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* CTA BUTTONS */
        .cta-container {
            display: flex; gap: 1.5rem; justify-content: center; flex-wrap: wrap;
            opacity: 0; animation: fadeSlideUp 0.8s ease forwards; animation-delay: 1.7s;
        }
        .cta-btn {
            position: relative; display: inline-flex; align-items: center;
            gap: 0.75rem; padding: 1.2rem 2.2rem;
            font-family: 'Syne', sans-serif; font-size: 1rem; font-weight: 600;
            text-decoration: none; color: var(--ghost-white);
            border: 1px solid rgba(96, 165, 250, 0.25); border-radius: 60px;
            background: linear-gradient(135deg, rgba(30, 58, 138, 0.15), rgba(10, 10, 46, 0.4));
            backdrop-filter: blur(10px);
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            overflow: hidden; cursor: pointer; letter-spacing: 0.04em;
        }
        .cta-btn::before {
            content: ''; position: absolute; top: 0; left: -100%;
            width: 100%; height: 100%;
            background: linear-gradient(90deg, transparent, rgba(96, 165, 250, 0.1), transparent);
            transition: left 0.6s ease;
        }
        .cta-btn:hover::before { left: 100%; }
        .cta-btn:hover {
            border-color: var(--glow-blue);
            box-shadow: 0 0 30px rgba(96, 165, 250, 0.15), inset 0 0 30px rgba(96, 165, 250, 0.05);
            transform: translateY(-2px);
        }
        .cta-btn:active { transform: translateY(0) scale(0.98); }
        .cta-icon { font-size: 1.3rem; transition: transform 0.3s ease; }
        .cta-btn:hover .cta-icon { transform: scale(1.2); }
        .cta-btn .arrow { font-size: 0.9rem; transition: transform 0.3s ease; opacity: 0.6; }
        .cta-btn:hover .arrow { transform: translateX(4px); opacity: 1; }

        /* SCROLL INDICATOR */
        .scroll-indicator {
            position: absolute; bottom: 2.5rem; left: 50%;
            transform: translateX(-50%); display: flex;
            flex-direction: column; align-items: center; gap: 0.5rem;
            opacity: 0;
            animation: fadeSlideUp 0.8s ease forwards, bobUp 2s ease-in-out infinite;
            animation-delay: 2.5s, 3s; z-index: 2;
        }
        .scroll-indicator span {
            font-family: 'DM Mono', monospace; font-size: 0.65rem;
            letter-spacing: 0.25em; text-transform: uppercase; color: var(--dim-white);
        }
        .scroll-line {
            width: 1px; height: 40px;
            background: linear-gradient(to bottom, var(--glow-blue), transparent);
            animation: scrollPulse 2s ease-in-out infinite;
        }
        @keyframes bobUp {
            0%, 100% { transform: translateX(-50%) translateY(0); }
            50% { transform: translateX(-50%) translateY(-8px); }
        }
        @keyframes scrollPulse {
            0%, 100% { opacity: 0.3; }
            50% { opacity: 0.8; }
        }

        /* PASSWORD MODAL */
        .modal-overlay {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(5, 5, 16, 0.92); backdrop-filter: blur(20px);
            z-index: 40000; display: flex; align-items: center; justify-content: center;
            opacity: 0; pointer-events: none; transition: opacity 0.4s ease;
        }
        .modal-overlay.active { opacity: 1; pointer-events: all; }
        .modal {
            background: linear-gradient(165deg, rgba(15, 27, 77, 0.5), rgba(5, 5, 16, 0.8));
            border: 1px solid rgba(96, 165, 250, 0.15); border-radius: 20px;
            padding: 3rem; max-width: 450px; width: 90%; text-align: center;
            transform: translateY(20px) scale(0.95);
            transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1); position: relative;
        }
        .modal-overlay.active .modal { transform: translateY(0) scale(1); }
        .modal-lock { font-size: 2.5rem; margin-bottom: 1.5rem; animation: lockPulse 2s ease-in-out infinite; }
        @keyframes lockPulse { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.05); } }
        .modal h3 { font-family: 'Instrument Serif', serif; font-size: 1.8rem; font-weight: 400; margin-bottom: 0.5rem; }
        .modal p { font-family: 'DM Mono', monospace; font-size: 0.75rem; color: var(--dim-white); letter-spacing: 0.1em; margin-bottom: 2rem; }
        .password-input-wrap { position: relative; margin-bottom: 1.5rem; }
        .password-input {
            width: 100%; padding: 1rem 1.5rem;
            font-family: 'DM Mono', monospace; font-size: 0.9rem;
            color: var(--ghost-white); background: rgba(10, 10, 46, 0.6);
            border: 1px solid rgba(96, 165, 250, 0.2); border-radius: 12px;
            outline: none; transition: border-color 0.3s ease, box-shadow 0.3s ease;
            letter-spacing: 0.05em;
        }
        .password-input:focus { border-color: var(--glow-blue); box-shadow: 0 0 20px rgba(96, 165, 250, 0.1); }
        .password-input::placeholder { color: rgba(156, 163, 175, 0.5); }
        .password-error {
            color: #f87171; font-family: 'DM Mono', monospace; font-size: 0.75rem;
            margin-top: -0.5rem; margin-bottom: 1rem; opacity: 0; transition: opacity 0.3s; height: 1rem;
        }
        .password-error.show { opacity: 1; }
        .modal-submit {
            padding: 0.9rem 2rem; font-family: 'Syne', sans-serif;
            font-size: 0.9rem; font-weight: 600; color: var(--ghost-white);
            background: linear-gradient(135deg, var(--accent-blue), var(--mid-blue));
            border: none; border-radius: 50px; cursor: pointer;
            transition: all 0.3s ease; letter-spacing: 0.04em;
        }
        .modal-submit:hover { box-shadow: 0 0 30px rgba(30, 58, 138, 0.4); transform: translateY(-1px); }
        .modal-close {
            position: absolute; top: 1rem; right: 1.5rem;
            background: none; border: none; color: var(--dim-white);
            font-size: 1.5rem; cursor: pointer; transition: color 0.3s; line-height: 1;
        }
        .modal-close:hover { color: var(--ghost-white); }

        /* SECTION CONTENT AREAS */
        .screenwriting-content,
        .copywriting-content,
        .personal-projects-content {
            min-height: 100vh; padding: 6rem 2rem; display: none; position: relative;
        }
        .screenwriting-content.unlocked { display: block; }
        .copywriting-content.active { display: block; }
        .personal-projects-content.active { display: block; }

        .content-header { text-align: center; margin-bottom: 4rem; }
        .content-header h2 {
            font-family: 'Instrument Serif', serif;
            font-size: clamp(2.5rem, 6vw, 4rem);
            font-weight: 400; margin-bottom: 1rem;
        }
        .content-header p {
            font-family: 'DM Mono', monospace; font-size: 0.8rem;
            color: var(--dim-white); letter-spacing: 0.15em;
        }

        /* WORK GRID */
        .work-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 2rem; max-width: 1000px; margin: 0 auto;
        }
        .work-item {
            background: linear-gradient(165deg, rgba(15, 27, 77, 0.25), rgba(5, 5, 16, 0.5));
            border: 1px solid rgba(96, 165, 250, 0.08); border-radius: 16px;
            padding: 2rem; transition: all 0.3s ease; cursor: default;
        }
        .work-item:hover {
            border-color: rgba(96, 165, 250, 0.2);
            transform: translateY(-4px); box-shadow: 0 20px 40px rgba(0,0,0,0.3);
        }
        .work-item-placeholder {
            width: 100%; height: 150px;
            background: linear-gradient(135deg, rgba(30, 58, 138, 0.1), rgba(10, 10, 46, 0.3));
            border-radius: 10px; margin-bottom: 1.5rem; display: flex;
            align-items: center; justify-content: center;
            font-family: 'DM Mono', monospace; font-size: 0.7rem;
            color: var(--dim-white); letter-spacing: 0.15em; text-transform: uppercase;
        }
        .work-item h3 { font-family: 'Syne', sans-serif; font-size: 1.1rem; font-weight: 600; margin-bottom: 0.5rem; }
        .work-item p { font-size: 0.85rem; color: var(--dim-white); line-height: 1.6; }

        /* VIDEO GRID */
        .video-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 2rem; max-width: 1100px; margin: 0 auto;
        }
        .video-card {
            background: linear-gradient(165deg, rgba(15, 27, 77, 0.25), rgba(5, 5, 16, 0.5));
            border: 1px solid rgba(96, 165, 250, 0.08); border-radius: 16px;
            padding: 1.5rem; transition: all 0.3s ease;
        }
        .video-card:hover {
            border-color: rgba(96, 165, 250, 0.2);
            transform: translateY(-4px); box-shadow: 0 20px 40px rgba(0,0,0,0.3);
        }
        .video-card video {
            width: 100%; border-radius: 10px;
            display: block; margin-bottom: 1.2rem;
            background: #000;
            max-height: 240px; object-fit: contain;
        }
        .video-card h3 { font-family: 'Syne', sans-serif; font-size: 1rem; font-weight: 600; margin-bottom: 0.4rem; }
        .video-card p { font-size: 0.8rem; color: var(--dim-white); line-height: 1.5; }
        .video-label {
            display: inline-block; margin-bottom: 0.8rem;
            font-family: 'DM Mono', monospace; font-size: 0.65rem;
            letter-spacing: 0.2em; text-transform: uppercase;
            color: var(--glow-blue); padding: 0.2rem 0.6rem;
            border: 1px solid rgba(96, 165, 250, 0.2); border-radius: 20px;
        }

        /* BACK BUTTON */
        .back-btn {
            position: fixed; top: 2rem; left: 2rem; z-index: 100;
            display: none; align-items: center; gap: 0.5rem;
            padding: 0.7rem 1.2rem;
            font-family: 'DM Mono', monospace; font-size: 0.75rem;
            color: var(--dim-white); background: rgba(5, 5, 16, 0.7);
            border: 1px solid rgba(96, 165, 250, 0.15); border-radius: 50px;
            cursor: pointer; transition: all 0.3s; backdrop-filter: blur(10px);
            text-decoration: none; letter-spacing: 0.1em;
        }
        .back-btn:hover { color: var(--ghost-white); border-color: var(--glow-blue); }
        .back-btn.visible { display: inline-flex; }

        /* FOOTER */
        footer { padding: 3rem 2rem; text-align: center; position: relative; z-index: 2; }
        .footer-line { width: 40px; height: 1px; background: rgba(96, 165, 250, 0.3); margin: 0 auto 1.5rem; }
        footer p { font-family: 'DM Mono', monospace; font-size: 0.7rem; color: var(--dim-white); letter-spacing: 0.15em; }

        /* TYPEWRITER CURSOR */
        .typewriter-cursor {
            display: inline-block; width: 2px; height: 1em;
            background: var(--glow-blue); margin-left: 2px;
            animation: blink 1s step-end infinite; vertical-align: text-bottom;
        }
        @keyframes blink { 0%, 50% { opacity: 1; } 51%, 100% { opacity: 0; } }

        /* PAGE TRANSITIONS */
        .page-main { transition: opacity 0.5s ease; }
        .page-main.hidden { opacity: 0; pointer-events: none; display: none; }

        /* EASTER EGG */
        body.rainbow-mode { animation: rainbowBg 3s linear infinite; }
        @keyframes rainbowBg {
            0%{background:#050510} 25%{background:#0a0520} 50%{background:#050a20} 75%{background:#100520} 100%{background:#050510}
        }
        body.rainbow-mode .hero-name {
            background: linear-gradient(90deg, #f87171, #fbbf24, #34d399, #60a5fa, #a78bfa, #f87171);
            background-size: 300% auto; -webkit-background-clip: text;
            -webkit-text-fill-color: transparent; background-clip: text;
            animation: rainbowText 2s linear infinite;
        }
        @keyframes rainbowText { 0%{background-position:0% center} 100%{background-position:300% center} }
        .secret-toast {
            position: fixed; bottom: 2rem; left: 50%;
            transform: translateX(-50%) translateY(100px);
            padding: 1rem 2rem; font-family: 'DM Mono', monospace; font-size: 0.8rem;
            color: var(--glow-blue); background: rgba(10, 10, 46, 0.9);
            border: 1px solid rgba(96, 165, 250, 0.3); border-radius: 12px;
            z-index: 10001; transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            white-space: nowrap; backdrop-filter: blur(10px);
        }
        .secret-toast.show { transform: translateX(-50%) translateY(0); }

        /* RESPONSIVE */
        @media (max-width: 768px) {
            .hero { padding: 1.5rem; }
            .cta-container { flex-direction: column; align-items: center; gap: 1rem; }
            .cta-btn { width: 100%; max-width: 300px; justify-content: center; }
            .cursor-dot, .cursor-ring { display: none; }
            .modal { padding: 2rem; }
            .video-grid { grid-template-columns: 1fr; }
        }
        @media (max-width: 480px) {
            .hero-blurb { font-size: 0.9rem; }
        }
    </style>
</head>
<body>

    <!-- =============================================
         ENTRY SCREEN
    ============================================= -->
    <div id="entryScreen">
        <div class="entry-orb entry-orb-1"></div>
        <div class="entry-orb entry-orb-2"></div>
        <div class="entry-orb entry-orb-3"></div>

        <div class="entry-card-container">
            <div class="entry-card">
                <div class="entry-icon-row" id="entryIcon">🤔</div>
                <h1 class="entry-question">Are you going to hire Aaron for a role in your company?</h1>
                <p class="entry-subtitle" id="entrySubtitle">Choose wisely. There is only one correct answer.</p>

                <div class="entry-arena" id="entryArena">
                    <button class="entry-btn entry-btn-yes" id="entryYesBtn">Yes</button>
                    <button class="entry-btn entry-btn-no" id="entryNoBtn">No</button>
                </div>
                <div class="entry-dodge-counter" id="entryCounter"></div>

                <div class="entry-confession" id="entryConfession">
                    <p>Excellent decision. You clearly have impeccable judgment. ✨</p>
                    <button class="entry-portfolio-btn" id="entryPortfolioBtn">View Aaron's Portfolio →</button>
                </div>
            </div>
        </div>
    </div>

    <!-- =============================================
         PORTFOLIO SCREEN
    ============================================= -->
    <div id="portfolioScreen">

        <!-- Custom Cursor -->
        <div class="cursor-dot" id="cursorDot"></div>
        <div class="cursor-ring" id="cursorRing"></div>

        <!-- Particles -->
        <div class="particles-container" id="particles"></div>

        <!-- Secret Toast -->
        <div class="secret-toast" id="secretToast">☕ You found a secret! Hire this guy.</div>

        <!-- Back Button -->
        <a href="#" class="back-btn" id="backBtn" onclick="goHome(event)">← Back</a>

        <!-- PASSWORD MODAL -->
        <div class="modal-overlay" id="passwordModal">
            <div class="modal">
                <button class="modal-close" onclick="closeModal()">&times;</button>
                <div class="modal-lock">🔐</div>
                <h3>Protected Work</h3>
                <p>This section contains confidential screenwriting samples.</p>
                <div class="password-input-wrap">
                    <input type="password" class="password-input" id="passwordInput"
                           placeholder="Enter passphrase..."
                           onkeydown="if(event.key==='Enter')checkPassword()">
                </div>
                <div class="password-error" id="passwordError">Incorrect passphrase. Try again.</div>
                <button class="modal-submit" onclick="checkPassword()">Unlock</button>
            </div>
        </div>

        <!-- MAIN PAGE -->
        <div class="page-main" id="pageMain">

            <!-- HERO -->
            <section class="hero" id="hero">
                <div class="hero-bg-gradient"></div>
                <div class="orb orb-1"></div>
                <div class="orb orb-2"></div>
                <div class="orb orb-3"></div>

                <div class="hero-content">
                    <div class="greeting">Welcome — you've found</div>
                    <h1 class="hero-name" id="heroName">Aaron Lattimore</h1>
                    <div class="hero-title">Screenwriter  ·  Copywriter  ·  Storyteller</div>
                    <p class="hero-blurb">
                        Screenwriter with over 8 years of experience in crafting high-quality scripts for visual media.
                        Proficient in transforming complex narratives into compelling dialogue, collaborating closely
                        with production teams to produce engaging content. Eager to leverage skills in storytelling
                        and content creation to drive impactful performance marketing initiatives.
                    </p>

                    <div class="cta-container">
                        <a href="#" class="cta-btn" onclick="openScreenwriting(event)">
                            <span class="cta-icon">🎬</span>
                            Screenwriting
                            <span class="arrow">→</span>
                        </a>
                        <a href="#" class="cta-btn" onclick="openCopywriting(event)">
                            <span class="cta-icon">✍️</span>
                            Copywriting
                            <span class="arrow">→</span>
                        </a>
                        <a href="#" class="cta-btn" onclick="openPersonalProjects(event)">
                            <span class="cta-icon">🚀</span>
                            Personal Projects
                            <span class="arrow">→</span>
                        </a>
                    </div>
                </div>

                <div class="scroll-indicator">
                    <span>Explore</span>
                    <div class="scroll-line"></div>
                </div>
            </section>

            <!-- FOOTER -->
            <footer>
                <div class="footer-line"></div>
                <p>© 2026 Aaron Lattimore · Built with care<span class="typewriter-cursor"></span></p>
            </footer>
        </div>

        <!-- SCREENWRITING CONTENT (password protected) -->
        <div class="screenwriting-content" id="screenwritingContent">
            <div class="content-header">
                <h2>Screenwriting</h2>
                <p>Scripts · Treatments · Narrative Work</p>
            </div>

            <div class="video-grid">
                <div class="video-card">
                    <span class="video-label">Commercial</span>
                    <video
                        controls
                        controlsList="nodownload nofullscreen"
                        oncontextmenu="return false;"
                        playsinline
                        preload="metadata">
                        <source src="com-rep23_E_03_r720P.mp4" type="video/mp4">
                        Your browser does not support the video tag.
                    </video>
                    <h3>Commercial Reel — Spot 03</h3>
                    <p>Commercial script and production sample from the 2023 reel.</p>
                </div>

                <div class="video-card">
                    <span class="video-label">Commercial</span>
                    <video
                        controls
                        controlsList="nodownload nofullscreen"
                        oncontextmenu="return false;"
                        playsinline
                        preload="metadata">
                        <source src="com-rep23_E_06_r720P.mp4" type="video/mp4">
                        Your browser does not support the video tag.
                    </video>
                    <h3>Commercial Reel — Spot 06</h3>
                    <p>Commercial script and production sample from the 2023 reel.</p>
                </div>

                <div class="video-card">
                    <span class="video-label">Original Work</span>
                    <video
                        controls
                        controlsList="nodownload nofullscreen"
                        oncontextmenu="return false;"
                        playsinline
                        preload="metadata">
                        <source src="osg_E_001_r720P.mp4" type="video/mp4">
                        Your browser does not support the video tag.
                    </video>
                    <h3>OSG — Episode 001</h3>
                    <p>Original scripted project, episode one.</p>
                </div>

                <div class="video-card">
                    <span class="video-label">Production</span>
                    <video
                        controls
                        controlsList="nodownload nofullscreen"
                        oncontextmenu="return false;"
                        playsinline
                        preload="metadata">
                        <source src="501100047_E_cnt_1_r720P.mp4" type="video/mp4">
                        Your browser does not support the video tag.
                    </video>
                    <h3>Project 501100047 — Cut 1</h3>
                    <p>Scripted production sample, content cut one.</p>
                </div>
            </div>
        </div>

        <!-- COPYWRITING CONTENT -->
        <div class="copywriting-content" id="copywritingContent">
            <div class="content-header">
                <h2>Copywriting</h2>
                <p>Performance Marketing · Brand Voice · Content Strategy</p>
            </div>
            <div class="work-grid">
                <div class="work-item">
                    <div class="work-item-placeholder">Coming Soon</div>
                    <h3>Project Title</h3>
                    <p>Add your copywriting samples here. Campaign details, metrics, and the story behind the work.</p>
                </div>
                <div class="work-item">
                    <div class="work-item-placeholder">Coming Soon</div>
                    <h3>Project Title</h3>
                    <p>Add your copywriting samples here. Campaign details, metrics, and the story behind the work.</p>
                </div>
                <div class="work-item">
                    <div class="work-item-placeholder">Coming Soon</div>
                    <h3>Project Title</h3>
                    <p>Add your copywriting samples here. Campaign details, metrics, and the story behind the work.</p>
                </div>
            </div>
        </div>

        <!-- PERSONAL PROJECTS CONTENT -->
        <div class="personal-projects-content" id="personalProjectsContent">
            <div class="content-header">
                <h2>Personal Projects</h2>
                <p>Side Projects · Passion Work · Experiments</p>
            </div>
            <div class="work-grid">
                <div class="work-item">
                    <div class="work-item-placeholder">Coming Soon</div>
                    <h3>Project Title</h3>
                    <p>Add your personal projects here. Share the story, the process, and what you learned along the way.</p>
                </div>
                <div class="work-item">
                    <div class="work-item-placeholder">Coming Soon</div>
                    <h3>Project Title</h3>
                    <p>Add your personal projects here. Share the story, the process, and what you learned along the way.</p>
                </div>
                <div class="work-item">
                    <div class="work-item-placeholder">Coming Soon</div>
                    <h3>Project Title</h3>
                    <p>Add your personal projects here. Share the story, the process, and what you learned along the way.</p>
                </div>
            </div>
        </div>

    </div><!-- /#portfolioScreen -->

    <script>
        // =============================================
        // ENTRY PAGE LOGIC
        // =============================================
        var entryYesBtn = document.getElementById('entryYesBtn');
        var entryNoBtn = document.getElementById('entryNoBtn');
        var entryArena = document.getElementById('entryArena');
        var entryCounter = document.getElementById('entryCounter');
        var entryConfession = document.getElementById('entryConfession');
        var entryPortfolioBtn = document.getElementById('entryPortfolioBtn');

        var dodgeCount = 0, yesScale = 1, noScale = 1, entryClicked = false;

        var msgs = [
            "Nice try.",
            "Not so fast!",
            "You sure about that?",
            "Think again\u2026",
            "The button disagrees.",
            "Aaron deserves better.",
            "Wrong answer \u2014 blocked!",
            "That button has trust issues.",
            "It\u2019s not you, it\u2019s the button.",
            "The No button has left the chat.",
            "Reconsider? The button insists.",
            "Error 403: Bad Decision Forbidden."
        ];

        function dodgeNo() {
            if (entryClicked) return;
            dodgeCount++;
            noScale = Math.max(0.2, noScale - 0.07);
            yesScale = Math.min(2.5, yesScale + 0.1);
            entryNoBtn.style.transform = 'scale(' + noScale + ')';
            entryNoBtn.style.opacity = Math.max(0.2, 1 - dodgeCount * 0.06);
            entryYesBtn.style.transform = 'scale(' + yesScale + ')';
            entryYesBtn.style.boxShadow = '0 4px ' + (24 + dodgeCount * 4) + 'px rgba(34,197,94,' + Math.min(0.65, 0.35 + dodgeCount * 0.03) + ')';
            entryNoBtn.style.position = 'absolute';
            var aw = entryArena.offsetWidth;
            var ah = Math.max(entryArena.offsetHeight, 80 + dodgeCount * 14);
            var bw = entryNoBtn.offsetWidth * noScale;
            var bh = entryNoBtn.offsetHeight * noScale;
            entryNoBtn.style.left = (16 + Math.random() * Math.max(aw - bw - 32, 30)) + 'px';
            entryNoBtn.style.top = (16 + Math.random() * Math.max(ah - bh - 32, 10)) + 'px';
            entryArena.style.minHeight = (80 + dodgeCount * 14) + 'px';
            entryCounter.classList.add('visible');
            var m = msgs[Math.min(dodgeCount - 1, msgs.length - 1)];
            entryCounter.textContent = m + '  (' + dodgeCount + ' dodge' + (dodgeCount > 1 ? 's' : '') + ')';
        }

        entryNoBtn.addEventListener('mouseenter', dodgeNo);
        entryNoBtn.addEventListener('touchstart', function(e) { e.preventDefault(); dodgeNo(); });

        entryYesBtn.addEventListener('click', function() {
            if (entryClicked) return;
            entryClicked = true;

            // Particle burst
            var rect = entryYesBtn.getBoundingClientRect();
            var cx = rect.left + rect.width / 2;
            var cy = rect.top + rect.height / 2;
            var colors = ['#22c55e','#6c63ff','#8b5cf6','#3b82f6','#fbbf24','#f472b6'];
            for (var i = 0; i < 30; i++) {
                var p = document.createElement('div');
                p.classList.add('entry-particle');
                var angle = (Math.PI * 2 * i) / 30;
                var dist = 60 + Math.random() * 140;
                p.style.left = cx + 'px'; p.style.top = cy + 'px';
                p.style.background = colors[i % colors.length];
                p.style.setProperty('--tx', Math.cos(angle) * dist + 'px');
                p.style.setProperty('--ty', Math.sin(angle) * dist + 'px');
                document.body.appendChild(p);
                setTimeout(function(el) { el.remove(); }, 900, p);
            }

            entryYesBtn.style.transform = 'scale(1.15)';
            entryYesBtn.style.boxShadow = '0 0 80px rgba(34,197,94,0.5)';

            setTimeout(function() {
                entryArena.style.display = 'none';
                entryCounter.style.display = 'none';
                entryConfession.classList.add('show');
                document.getElementById('entryIcon').textContent = '🎉';
                document.getElementById('entryIcon').style.fontSize = '48px';
                var sub = document.getElementById('entrySubtitle');
                if (dodgeCount > 0) {
                    sub.textContent = 'It only took ' + dodgeCount + ' dodge' + (dodgeCount > 1 ? 's' : '') + ' to reach the right answer.';
                } else {
                    sub.textContent = "You didn't even hesitate. Respect.";
                }
            }, 400);
        });

        entryPortfolioBtn.addEventListener('click', showPortfolio);

        function showPortfolio() {
            var entryScreen = document.getElementById('entryScreen');
            var portfolioScreen = document.getElementById('portfolioScreen');

            entryScreen.classList.add('fade-out');
            portfolioScreen.style.display = 'block';

            setTimeout(function() {
                portfolioScreen.classList.add('show');
                window.scrollTo({ top: 0, behavior: 'instant' });
                entryScreen.classList.add('gone');
                initPortfolio();
            }, 700);
        }

        // =============================================
        // PORTFOLIO LOGIC
        // =============================================
        var portfolioInitialized = false;

        function initPortfolio() {
            if (portfolioInitialized) return;
            portfolioInitialized = true;

            // Cursor tracking
            var dot = document.getElementById('cursorDot');
            var ring = document.getElementById('cursorRing');
            var mouseX = 0, mouseY = 0, ringX = 0, ringY = 0;

            document.addEventListener('mousemove', function(e) {
                mouseX = e.clientX; mouseY = e.clientY;
                dot.style.left = mouseX - 3 + 'px';
                dot.style.top = mouseY - 3 + 'px';
            });

            function animateRing() {
                ringX += (mouseX - ringX) * 0.12;
                ringY += (mouseY - ringY) * 0.12;
                ring.style.left = ringX - 18 + 'px';
                ring.style.top = ringY - 18 + 'px';
                requestAnimationFrame(animateRing);
            }
            animateRing();

            document.querySelectorAll('a, button, .cta-btn').forEach(function(el) {
                el.addEventListener('mouseenter', function() { ring.classList.add('hovering'); });
                el.addEventListener('mouseleave', function() { ring.classList.remove('hovering'); });
            });

            // Particles
            var particlesContainer = document.getElementById('particles');
            for (var i = 0; i < 30; i++) {
                var p = document.createElement('div');
                p.classList.add('particle');
                p.style.left = Math.random() * 100 + '%';
                p.style.animationDuration = (8 + Math.random() * 15) + 's';
                p.style.animationDelay = Math.random() * 10 + 's';
                p.style.width = (1 + Math.random() * 2) + 'px';
                p.style.height = p.style.width;
                particlesContainer.appendChild(p);
            }

            // Easter eggs
            var konamiCode = [38,38,40,40,37,39,37,39,66,65];
            var konamiIndex = 0;
            document.addEventListener('keydown', function(e) {
                if (e.keyCode === konamiCode[konamiIndex]) {
                    konamiIndex++;
                    if (konamiIndex === konamiCode.length) {
                        document.body.classList.toggle('rainbow-mode');
                        showToast('🌈 Rainbow mode activated! You know the code.');
                        konamiIndex = 0;
                    }
                } else { konamiIndex = 0; }
            });

            var nameClickCount = 0, nameClickTimer;
            document.getElementById('heroName').addEventListener('click', function() {
                nameClickCount++;
                clearTimeout(nameClickTimer);
                nameClickTimer = setTimeout(function() { nameClickCount = 0; }, 500);
                if (nameClickCount === 3) {
                    showToast('🎯 Persistence pays off. So does hiring Aaron.');
                    nameClickCount = 0;
                }
            });

            var typedKeys = '';
            document.addEventListener('keypress', function(e) {
                if (e.target.tagName === 'INPUT') return;
                typedKeys += e.key.toLowerCase();
                if (typedKeys.length > 20) typedKeys = typedKeys.slice(-20);
                if (typedKeys.includes('hire')) {
                    showToast("🎉 Great instinct. Let's make it official!");
                    typedKeys = '';
                }
            });

            console.log('%c👋 Hey there, inspector!', 'font-size: 20px; font-weight: bold; color: #60a5fa;');
            console.log('%cIf you\'re reading this, you\'re thorough. Aaron likes thorough people.', 'font-size: 14px; color: #9ca3af;');
            console.log('%c📧 Reach out — good things happen when curious people connect.', 'font-size: 14px; color: #3b82f6;');

            // Shake style
            var shakeStyle = document.createElement('style');
            shakeStyle.textContent = '@keyframes shake { 0%,100%{transform:translateX(0)} 20%{transform:translateX(-8px)} 40%{transform:translateX(8px)} 60%{transform:translateX(-4px)} 80%{transform:translateX(4px)} }';
            document.head.appendChild(shakeStyle);
        }

        // PASSWORD SYSTEM
        var CORRECT_PASS = 'humanitas in machina';

        function openScreenwriting(e) {
            e.preventDefault();
            document.getElementById('passwordModal').classList.add('active');
            setTimeout(function() { document.getElementById('passwordInput').focus(); }, 300);
        }

        function closeModal() {
            document.getElementById('passwordModal').classList.remove('active');
            document.getElementById('passwordInput').value = '';
            document.getElementById('passwordError').classList.remove('show');
        }

        function checkPassword() {
            var input = document.getElementById('passwordInput').value.trim().toLowerCase();
            if (input === CORRECT_PASS) {
                closeModal();
                showSection('screenwriting');
            } else {
                document.getElementById('passwordError').classList.add('show');
                document.getElementById('passwordInput').value = '';
                var modal = document.querySelector('.modal');
                modal.style.animation = 'shake 0.4s ease';
                setTimeout(function() { modal.style.animation = ''; }, 400);
            }
        }

        function openCopywriting(e) {
            e.preventDefault();
            showSection('copywriting');
        }

        function openPersonalProjects(e) {
            e.preventDefault();
            showSection('personalProjects');
        }

        function showSection(section) {
            document.getElementById('pageMain').classList.add('hidden');
            document.getElementById('backBtn').classList.add('visible');

            document.getElementById('screenwritingContent').classList.remove('unlocked');
            document.getElementById('copywritingContent').classList.remove('active');
            document.getElementById('personalProjectsContent').classList.remove('active');

            if (section === 'screenwriting') {
                document.getElementById('screenwritingContent').classList.add('unlocked');
            } else if (section === 'copywriting') {
                document.getElementById('copywritingContent').classList.add('active');
            } else if (section === 'personalProjects') {
                document.getElementById('personalProjectsContent').classList.add('active');
            }

            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        function goHome(e) {
            e.preventDefault();
            document.getElementById('pageMain').classList.remove('hidden');
            document.getElementById('backBtn').classList.remove('visible');
            document.getElementById('screenwritingContent').classList.remove('unlocked');
            document.getElementById('copywritingContent').classList.remove('active');
            document.getElementById('personalProjectsContent').classList.remove('active');
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        document.getElementById('passwordModal').addEventListener('click', function(e) {
            if (e.target === document.getElementById('passwordModal')) closeModal();
        });

        function showToast(msg) {
            var toast = document.getElementById('secretToast');
            toast.textContent = msg;
            toast.classList.add('show');
            setTimeout(function() { toast.classList.remove('show'); }, 3500);
        }
    </script>

</body>
</html>
